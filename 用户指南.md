# 用户指南

## 1. 填入新数学问题

复制本工程作为一个新项目后，填写：

1. `notes/problem_statement.md` 中全部 `[请填写]`；
2. 将 `setup_status: empty` 改为 `setup_status: locked`；
3. 在 `literature/user_references.md` 填写文献；没有参考文献时明确写“无”，不要保留占位符。

然后运行：

```bash
./scripts/prepare_project.sh
```

该命令检查占位符、初始化研究状态，并创建本地锁题提交。未锁定时自动化不会猜测题目。

## 2. 启动、监控和停止

建议用有限预算首次启动：

```bash
./scripts/run_research_supervisor_loop.sh start --max-tokens 200000
```

查看完整状态：

```bash
./scripts/run_research_supervisor_loop.sh status
```

只看 token：

```bash
jq '{total_tokens,max_tokens,researcher_tokens,supervisor_tokens,researcher_runs,supervisor_runs,status,phase}' runs/research_supervisor_loop/orchestrator_state.json
```

连续监看：

```bash
watch -n 5 './scripts/run_research_supervisor_loop.sh status'
```

安全停止和恢复：

```bash
./scripts/stop_research_supervisor_loop.sh
./scripts/run_research_supervisor_loop.sh resume --max-tokens 400000
```

`max_tokens` 是累计输入 token 与输出 token 之和的批次间停止阈值。它在一次 Agent 完成后更新，因此不是单次调用的硬截断，最后一次调用可能略微越过阈值。恢复时填写新的累计上限，而非额外额度。

## 3. 结果位置

- 锁定题目：`notes/problem_statement.md`
- 用户文献：`literature/user_references.md`
- 附加假设：`notes/admissible_assumptions.md`
- 当前状态：`iteration_state.json`
- 正反向前沿：`frontiers/`
- 证明：`proof/`
- 失败审计：`audit/`
- 自动运行记录：`runs/research_supervisor_loop/`
- Git 研究历史：`git log --oneline --decorate`

